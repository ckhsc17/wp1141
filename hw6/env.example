LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token
LINE_CHANNEL_SECRET=your-line-channel-secret
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-2.0-flash
DEBUG_API_TOKEN=optional-debug-token
DATABASE_URL=postgresql://user:password@localhost:5432/booboo_db
LIFF_DASHBOARD_URL=https://liff.line.me/YOUR_DASHBOARD_LIFF_ID
LIFF_SETTINGS_URL=https://liff.line.me/YOUR_SETTINGS_LIFF_ID
#CRON_SECRET=optional-cron-secret

# Memory Provider 配置
# 選項：mem0, upstash, postgresql, none
MEMORY_PROVIDER=mem0

# Mem0 Hosted Platform (如果 MEMORY_PROVIDER=mem0)
MEM0_API_KEY=your-mem0-api-key

# Upstash Vector (如果 MEMORY_PROVIDER=upstash，未來使用)
UPSTASH_VECTOR_REST_URL=https://your-vector-url.upstash.io
UPSTASH_VECTOR_REST_TOKEN=your-token

# PostgreSQL (如果 MEMORY_PROVIDER=postgresql，未來使用)
# 使用現有的 DATABASE_URL