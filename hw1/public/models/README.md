# 3D Models

此資料夾用於存放 3D 模型檔案

## 檔案結構
- `earth.gltf` - 地球模型（關於我 section）
- `earth.bin` - 地球模型的二進制資料檔案（如果有的話）
- 相關的紋理檔案（.jpg、.png 等）

## 支援格式
- `.gltf` - GLTF 文字格式
- `.glb` - GLTF 二進制格式（推薦，檔案較小）

## 注意事項
1. GLTF 檔案可能會包含多個檔案（.gltf + .bin + 紋理檔案）
2. GLB 檔案是單一檔案，更容易管理
3. 檔案路徑在程式碼中會是 `/models/檔名`
